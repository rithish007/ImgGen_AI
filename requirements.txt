# Pod environment for the AI-pipeline smoke test.
# Install torch FIRST from the CUDA index, then the rest:
#
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cu132
#   pip install -r requirements.txt
#
# CUDA 12.8+ is required for Blackwell (RTX 5090). Pick a RunPod PyTorch
# template on cu128 or newer; the local venv is on cu132.

# --- core (versions match the verified local env) ---
# diffusers==0.39.0 is also the current latest stable PyPI release (checked
# 2026-08-06) and has had Qwen-Image support since 0.35.0 - no need for a git
# install. It was briefly bumped to git source for Z-Image-Turbo's
# ZImagePipeline (needs unreleased PRs), which broke flux2dev's quantization
# path (a diffusers dev-build TorchAoConfig gap, see generate.py's
# _build_quantization_config comment) for a model that never needed the
# newer diffusers at all. Z-Image-Turbo has been dropped entirely rather than
# keep carrying that risk for every other model in the comparison.
diffusers==0.39.0
transformers==5.14.1
huggingface_hub==1.25.1
safetensors==0.8.0
tokenizers==0.22.2
numpy==2.4.4
pillow==12.2.0

# --- required on the pod, not yet installed locally ---
# accelerate: needed by enable_model_cpu_offload() (Klein base is ~29GB on a 32GB card)
accelerate>=1.14.0
# torchao: fp8 weight-only quantization for the flux2dev generate.py model -
# its 32B transformer + 24B Mistral text encoder don't fit a 47GB card in
# plain bf16 otherwise. Needs compute capability >= 8.9 for native fp8 tensor
# cores (RTX 4090/6000 Ada, Hopper); not needed for klein or qwenimage.
torchao>=0.14.0
# scipy + opencv: Stage 3b physics transform
scipy>=1.18.0
opencv-python-headless>=5.0.0
# ultralytics: Stage 4 dataset assembly / Stage 5
ultralytics>=8.4.108
